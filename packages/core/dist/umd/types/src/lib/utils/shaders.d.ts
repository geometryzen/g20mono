export declare const shaders: {
    create: (gl: WebGLRenderingContext, source: string, type: 'VERTEX_SHADER' | 'FRAGMENT_SHADER') => WebGLShader;
    types: {
        vertex: string;
        fragment: string;
    };
    path: {
        vertex: string;
        fragment: string;
    };
    points: {
        vertex: string;
        fragment: string;
    };
};
