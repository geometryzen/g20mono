import { Matrix } from "../matrix";
export declare function compose_2d_3x3_transform(x: number, y: number, sx: number, sy: number, cos_φ: number, sin_φ: number, skewX: number, skewY: number, matrix: Matrix): void;
